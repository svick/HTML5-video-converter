<UserControl x:Class="Video_converter.ConvertDone"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition />
			<RowDefinition />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" FontSize="18" HorizontalAlignment="Center" Foreground="#003399" Margin="10">Převod dokončen</TextBlock>
		<ProgressBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Height="30" Margin="10" Value="100"  />
		<Button Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Bottom"  Margin="10" Name="Back" Click="Back_Click">
			<TextBlock Margin="10,1" FontSize="13">
				    Zpět
			</TextBlock>
		</Button>
		<Button Grid.Row="3" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Bottom"  Margin="10" Name="ShowFolder" Click="ShowFolder_Click">
			<TextBlock Margin="10,1" FontSize="13">
				    Zobrazit složku
			</TextBlock>
		</Button>
	</Grid>
</UserControl>
